<nav class="navbar navbar-expand-md bg-light border-bottom sticky-top">
  <div class="container">
    <!-- Brand -->
    <a class="navbar-brand fw-semibold text-dark" href="/listings">
      <i class="fa-solid fa-compass me-1"></i> WanderLand
    </a>

    <div class="search">
      <form class="d-flex">
        <input
          class="form-control me-2 sea"
          type="search"
          placeholder="Search For Destinations"
          aria-label="Search"
        />

        <button class="btn search-button" type="submit">
          <i class="fas fa-search search-icon"></i>Search 
        </button>
      </form>
    </div>

    <!-- Toggler -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Nav items -->
    <div
      class="collapse navbar-collapse justify-content-end"
      id="navbarNavAltMarkup"
    >
      <div class="navbar-nav align-items-center">
        <a class="nav-link text-dark fw-medium cool-underline" href="/listings"
          >All Listings</a
        >
        <a
          class="nav-link text-dark fw-medium cool-underline"
          href="/listings/new"
          >Add New List</a
        >

        <% if (!currUser) { %>
        <a class="nav-link text-dark fw-medium cool-underline" href="/login"
          ><b>Login</b></a
        >
        <a class="nav-link text-dark fw-medium cool-underline" href="/signup"
          ><b>Signup</b></a
        >
        <% } else { %>
        <a class="nav-link text-dark fw-medium cool-underline" href="/logout"
          ><b>Logout</b></a
        >
        <% } %>
      </div>
    </div>
  </div>
</nav>
